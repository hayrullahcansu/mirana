package routes

import (
	"net/http"

	"github.com/hayrullahcansu/mirana/core/mng"
)

//JoinRoomMode1Handler hnadles login requests and authorize user who is valid
func JoinRoomNormalGameHandler(gs *mng.GameServer, w http.ResponseWriter, r *http.Request) {

	// if _, ok := gs.RoomManager.GetMode1Room(); ok {

	// }
}
